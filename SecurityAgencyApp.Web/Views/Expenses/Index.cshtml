@model SecurityAgencyApp.Web.Models.Api.ExpenseListResponse
@{
    ViewData["Title"] = "Expenses Management";
}

<div class="page-header">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1 class="page-title">Expenses</h1>
            <p class="page-subtitle">Track agency expenses</p>
        </div>
        <a href="@Url.Action("Create")" class="btn btn-primary">
            <i class="fas fa-receipt me-2"></i>Add Expense
        </a>
    </div>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle me-2"></i>@TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card border-0 shadow-sm">
    <div class="card-body">
        @if (Model?.Items != null && Model.Items.Any())
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Expense Number</th>
                            <th>Date</th>
                            <th>Category</th>
                            <th>Description</th>
                            <th>Site/Guard</th>
                            <th class="text-end">Amount</th>
                            <th>Status</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var expense in Model.Items)
                        {
                            <tr>
                                <td><strong>@expense.ExpenseNumber</strong></td>
                                <td>@expense.ExpenseDate.ToString("dd MMM yyyy")</td>
                                <td>@expense.Category</td>
                                <td>@expense.Description</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(expense.SiteName))
                                    {
                                        <span class="badge bg-info">@expense.SiteName</span>
                                    }
                                    @if (!string.IsNullOrEmpty(expense.GuardName))
                                    {
                                        <span class="badge bg-secondary">@expense.GuardName</span>
                                    }
                                    @if (string.IsNullOrEmpty(expense.SiteName) && string.IsNullOrEmpty(expense.GuardName))
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td class="text-end"><strong>₹@expense.Amount.ToString("N2")</strong></td>
                                <td>
                                    <span class="badge bg-@(expense.Status == "Approved" ? "success" : expense.Status == "Paid" ? "info" : "warning")">
                                        @expense.Status
                                    </span>
                                </td>
                                <td class="text-end">
                                    <a href="#" class="btn btn-sm btn-outline-primary" title="View">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @if (Model.TotalPages > 1)
            {
                <nav aria-label="Page navigation" class="mt-4">
                    <ul class="pagination justify-content-center">
                        @if (Model.PageNumber > 1)
                        {
                            <li class="page-item">
                                <a class="page-link" href="@Url.Action("Index", new { pageNumber = Model.PageNumber - 1, pageSize = Model.PageSize })">
                                    <i class="fas fa-chevron-left"></i> Previous
                                </a>
                            </li>
                        }
                        @for (int i = Math.Max(1, Model.PageNumber - 2); i <= Math.Min(Model.TotalPages, Model.PageNumber + 2); i++)
                        {
                            <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                                <a class="page-link" href="@Url.Action("Index", new { pageNumber = i, pageSize = Model.PageSize })">@i</a>
                            </li>
                        }
                        @if (Model.PageNumber < Model.TotalPages)
                        {
                            <li class="page-item">
                                <a class="page-link" href="@Url.Action("Index", new { pageNumber = Model.PageNumber + 1, pageSize = Model.PageSize })">
                                    Next <i class="fas fa-chevron-right"></i>
                                </a>
                            </li>
                        }
                    </ul>
                </nav>
            }
        }
        else
        {
            <div class="text-center py-5">
                <i class="fas fa-receipt fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">No expenses found</h5>
                <a href="@Url.Action("Create")" class="btn btn-primary mt-3">
                    <i class="fas fa-receipt me-2"></i>Add Expense
                </a>
            </div>
        }
    </div>
</div>
