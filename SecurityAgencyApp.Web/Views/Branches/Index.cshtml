@{
    ViewData["Title"] = "Branches";
    ViewData["Breadcrumbs"] = new[] { ("Branches", (string?)null) };
    var search = ViewBag.Search as string;
}

<div id="branchesIndexPage"
     data-index-url="@Url.Action("Index")"
     data-create-url="@Url.Action("Create")"
     data-details-base="@Url.Action("Details", new { id = "__id__" })"
     data-edit-base="@Url.Action("Edit", new { id = "__id__" })">
<div class="page-header">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
        <div>
            <h1 class="page-title">Branches</h1>
            <p class="page-subtitle mb-0">Manage operational branches used by sites, numbering, and compliance.</p>
        </div>
        <a href="@Url.Action("Create")" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Add Branch
        </a>
    </div>
</div>

<div class="row g-3 mb-4">
    <div class="col-12 col-md-6 col-xl-4">
        <div class="card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Total Branches</div>
                <div class="stat-value" data-stat="total">0</div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-xl-4">
        <div class="card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Active Branches</div>
                <div class="stat-value" data-stat="active">0</div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-xl-4">
        <div class="card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Head Offices</div>
                <div class="stat-value" data-stat="headOffice">0</div>
            </div>
        </div>
    </div>
</div>

<div class="card border-0 shadow-sm overflow-hidden">
    <div class="card-header bg-body-tertiary py-3">
        <h5 class="mb-0"><i class="fas fa-filter me-2 text-primary"></i>Search & Filters</h5>
    </div>
    <form id="branchesFilterForm" method="get" action="@Url.Action("Index")" class="table-toolbar">
        <div class="search-wrap">
            <div class="input-group">
                <span class="input-group-text bg-white border-end-0"><i class="fas fa-search text-muted"></i></span>
                <input type="text" name="search" class="form-control border-start-0" placeholder="Search by branch code, name, city..." value="@search" />
            </div>
        </div>
        <button type="submit" class="btn btn-primary btn-sm">
            <i class="fas fa-filter me-1"></i>Apply
        </button>
        @if (!string.IsNullOrWhiteSpace(search))
        {
            <a href="@Url.Action("Index")" class="btn btn-outline-secondary btn-sm">Clear</a>
        }
    </form>

    <div class="card-body p-0">
        <div class="px-4 py-3 border-bottom bg-body-tertiary d-flex justify-content-between align-items-center flex-wrap gap-2">
            <h5 class="mb-0"><i class="fas fa-code-branch me-2 text-primary"></i>Branch Directory</h5>
            <span id="branchesRecordCount" class="text-muted small">0 total records</span>
        </div>
        <div id="branchesTableWrapper"></div>
        <div id="branchesPaginationContainer" class="d-none px-4 py-3 border-top">
            <div id="branchesPaginationSummary" class="text-muted small mb-2"></div>
            <ul id="branchesPagination" class="pagination pagination-sm mb-0"></ul>
        </div>
    </div>
</div>
</div>

@section Scripts {
    <script src="~/js/pages/branches/index.js" asp-append-version="true"></script>
}
