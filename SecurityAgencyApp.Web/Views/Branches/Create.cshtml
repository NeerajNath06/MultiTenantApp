@model SecurityAgencyApp.Model.Api.CreateBranchRequest
@{
    ViewData["Title"] = "Create Branch";
    ViewData["Breadcrumbs"] = new[] { ("Branches", Url.Action("Index")), ("Create Branch", (string?)null) };
}

<div class="page-header">
    <h1 class="page-title">Create Branch</h1>
    <p class="page-subtitle mb-0">Add a new branch for site operations and numbering.</p>
</div>

<div class="card border-0 shadow-sm enterprise-form-card">
    <div class="card-body">
        <div id="branchFormPage"
             data-mode="create"
             data-submit-endpoint="Branches"
             data-redirect-url="@Url.Action("Index")">
        <form id="branchForm" class="form">
            <div id="branchFormErrors" class="alert alert-danger d-none" role="alert"></div>
            <partial name="_BranchFormFields" model="Model" />
            <div class="form-actions">
                <button id="branchFormSubmitButton" type="submit" class="btn btn-primary">
                    <i class="fas fa-save me-2"></i>Create Branch
                </button>
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                    <i class="fas fa-times me-2"></i>Cancel
                </a>
            </div>
        </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/pages/branches/form.js" asp-append-version="true"></script>
}
