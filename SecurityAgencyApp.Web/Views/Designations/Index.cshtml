@model SecurityAgencyApp.Model.Api.DesignationListResponse
@{
    ViewData["Title"] = "Designations Management";
    var currentSort = Context.Request.Query["sortBy"].ToString();
    var currentDirection = string.IsNullOrWhiteSpace(Context.Request.Query["sortDirection"]) ? "asc" : Context.Request.Query["sortDirection"].ToString();
    var currentSearch = Context.Request.Query["search"].ToString();
    var currentPageSize = string.IsNullOrWhiteSpace(Context.Request.Query["pageSize"]) ? "10" : Context.Request.Query["pageSize"].ToString();
}

<div class="page-header">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1 class="page-title">Designations</h1>
            <p class="page-subtitle mb-0">Manage job designations</p>
        </div>
        <a href="@Url.Action("Create")" class="btn btn-primary">
            <i class="fas fa-briefcase me-2"></i>Add New Designation
        </a>
    </div>
</div>

<div class="row g-3 mb-4" id="designationsStats">
    <div class="col-12 col-md-6 col-xl-4">
        <div class="card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Total Designations</div>
                <div class="stat-value" data-stat="total">0</div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-xl-4">
        <div class="card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Active Designations</div>
                <div class="stat-value" data-stat="active">0</div>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 col-xl-4">
        <div class="card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Linked Departments</div>
                <div class="stat-value" data-stat="departments">0</div>
            </div>
        </div>
    </div>
</div>

<div class="card border-0 shadow-sm mb-4">
    <div class="card-header bg-body-tertiary py-3">
        <h5 class="mb-0"><i class="fas fa-filter me-2 text-primary"></i>Search & Filters</h5>
    </div>
    <div class="card-body">
        <form method="get" class="row g-3" id="designationsFilterForm">
            <input type="hidden" name="pageNumber" value="1" />
            <input type="hidden" name="pageSize" value="@currentPageSize" />
            <input type="hidden" name="sortBy" value="@currentSort" />
            <input type="hidden" name="sortDirection" value="@currentDirection" />
            <div class="col-md-10">
                <div class="input-group">
                    <span class="input-group-text"><i class="fas fa-search"></i></span>
                    <input type="text" name="search" class="form-control" placeholder="Search by name, code, description..." value="@currentSearch" />
                </div>
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="fas fa-filter me-2"></i>Search
                </button>
            </div>
        </form>
    </div>
</div>

<div class="card border-0 shadow-sm overflow-hidden" id="designationsIndexPage"
     data-index-url="@Url.Action("Index", "Designations")"
     data-edit-base="@Url.Action("Edit", "Designations", new { id = "__id__" })">
    <div class="card-header bg-body-tertiary py-3 d-flex justify-content-between align-items-center flex-wrap gap-2">
        <h5 class="mb-0"><i class="fas fa-briefcase me-2 text-primary"></i>Designation Directory</h5>
        <span class="text-muted small" id="designationsRecordCount">0 total records</span>
    </div>
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
            <div>
                <span class="text-muted" id="designationsShowingSummary">Loading designations...</span>
            </div>
            <div>
                <select class="form-select form-select-sm d-inline-block" style="width: auto;" id="designationsPageSize">
                    <option value="10" selected="@(currentPageSize == "10")">10 per page</option>
                    <option value="25" selected="@(currentPageSize == "25")">25 per page</option>
                    <option value="50" selected="@(currentPageSize == "50")">50 per page</option>
                    <option value="100" selected="@(currentPageSize == "100")">100 per page</option>
                </select>
            </div>
        </div>
        <div id="designationsTableWrapper">
            <div class="text-center py-5">
                <i class="fas fa-briefcase fa-3x text-muted mb-3"></i>
                <h5 class="text-muted">Loading designations...</h5>
                <p class="text-muted">Fetching latest data from API.</p>
            </div>
        </div>
        <nav aria-label="Page navigation" class="mt-4 d-none" id="designationsPaginationContainer">
            <div class="text-center text-muted mb-2" id="designationsPaginationSummary"></div>
            <ul class="pagination justify-content-center" id="designationsPagination"></ul>
        </nav>
    </div>
</div>

@section Scripts {
    <script src="~/js/pages/designations/index.js" asp-append-version="true"></script>
}
