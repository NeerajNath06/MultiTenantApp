@model SecurityAgencyApp.Application.Features.Authentication.Commands.Login.LoginCommand
@{
    ViewData["Title"] = "Login";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Security Agency Management</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        * { box-sizing: border-box; }
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            min-height: 100dvh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 16px;
            margin: 0;
        }
        .login-container {
            max-width: 450px;
            width: 100%;
            padding: 12px;
        }
        .login-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
        }
        .login-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 28px 20px;
            text-align: center;
        }
        .login-header i {
            font-size: 40px;
            margin-bottom: 10px;
        }
        .login-header h2 {
            margin: 0;
            font-weight: 600;
            font-size: 1.5rem;
        }
        .login-header p {
            font-size: 0.9rem;
            margin-top: 6px;
        }
        .login-body {
            padding: 24px 20px;
        }
        .form-control {
            border-radius: 10px;
            padding: 12px 14px;
            min-height: 48px;
            font-size: 16px;
            border: 2px solid #e5e7eb;
            transition: all 0.2s;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .btn-login {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 10px;
            padding: 14px;
            min-height: 48px;
            font-weight: 600;
            font-size: 1rem;
            color: white;
            width: 100%;
            transition: transform 0.2s;
        }
        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
            color: white;
        }
        .form-check-input:checked {
            background-color: #667eea;
            border-color: #667eea;
        }
        .text-danger { color: #ef4444 !important; }
        @@media (max-width: 375px) {
            body { padding: 10px; }
            .login-container { padding: 8px; }
            .login-header { padding: 22px 16px; }
            .login-header i { font-size: 36px; }
            .login-header h2 { font-size: 1.35rem; }
            .login-body { padding: 20px 16px; }
        }
    </style>
</head>
<body class="auth-page auth-login">
    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <i class="fas fa-shield-alt"></i>
                <h2>Security Agency</h2>
                <p class="mb-0 mt-2">Management System</p>
            </div>
            <div class="login-body">
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger" role="alert">
                        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <div>@error.ErrorMessage</div>
                        }
                    </div>
                }
                
                <form method="post" asp-action="Login" asp-controller="Auth">
                    <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />
                    
                    <div class="mb-3">
                        <label asp-for="UserName" class="form-label fw-semibold">Username or Email</label>
                        <input asp-for="UserName" class="form-control" placeholder="Enter your username or email" required autofocus />
                        <span asp-validation-for="UserName" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label fw-semibold">Password</label>
                        <input asp-for="Password" type="password" class="form-control" placeholder="Enter your password" required />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <a href="@Url.Action("ForgotPassword", "Auth")" class="text-decoration-none small">Forgot password?</a>
                        <span class="text-muted mx-1">|</span>
                        <a href="@Url.Action("ResetPassword", "Auth")" class="text-decoration-none small">Reset password</a>
                    </div>
                    <div class="mb-4">
                        <div class="form-check">
                            <input asp-for="RememberMe" class="form-check-input" type="checkbox" />
                            <label asp-for="RememberMe" class="form-check-label">
                                Remember me
                            </label>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-login">
                        <i class="fas fa-sign-in-alt me-2"></i>Sign In
                    </button>
                </form>

                <div class="text-center mt-4">
                    <p class="text-muted mb-0">Don't have an account?</p>
                    <a href="@Url.Action("Register", "Auth")" class="text-decoration-none fw-semibold">
                        <i class="fas fa-building me-1"></i>Register Your Agency
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>
